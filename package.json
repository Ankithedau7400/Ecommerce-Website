{
  "homepage": "https://Ankithedau7400.github.io/Ecommerce-Website",
  "name": "ibook",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "cd backend && npm install && cd ../ibook && npm install && npm run build",
    "deploy":"gh-pages -d build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Ankit Hedau",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^4.0.0"
  }
}
